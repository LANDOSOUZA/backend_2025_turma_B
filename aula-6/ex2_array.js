// exemploo 2 com array
notas = [5, 10, 7.5];
console.log(notas);// exibe a lista de notas

notas.pop(); //remove a última nota do array
console.log(notas);

notas.push(9); // adiciona uma nota no final do array
console.log(notas);

notas.push(10);
console.log(notas); // adiciona uma nota no final do array
